<div class="flash">
    <%= flash[:notice1] %>
    <%= flash[:notice2] %>
</div>

<p>Title:<%= @book.title %></p>

<p>Body:<%= @book.body %></p>

<div class="button">
<%= link_to "Edit", edit_book_path(@book.id) %>
|
<%= link_to "Back", books_path(@book.id) %>
</div>
